1. add https://www.worldmonitor.io as source. use api to get news.
2. add "pinned" queries option. when user pinn a query, it should become a "case". make sure each user see its own cases.
3. when query become a "case" then, offer the user to schedule a periodic fetching of news related to this case.
4. when fetching new data to the case, validate, and summerize it, and add it to the case. make sure you add only new information. 
5. when adding new information to the  case, put a timeline of facts and events.
6. replace the first screen when page is laoded to world overview and layers (like worldmonitor.io) and give the user path to query page